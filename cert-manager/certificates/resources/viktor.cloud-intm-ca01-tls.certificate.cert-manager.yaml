# Certificate for intermediate CA 01
apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: viktor.cloud-intm-ca01-tls
  namespace: cert-manager
spec:
  isCA: true
  commonName: viktor.cloud-intm-ca01-tls
  secretName: viktor.cloud-intm-ca01-tls
  duration: 4380h # 6 months
  renewBefore: 360h # Certificates must be renewed some time before they expire (15 days)
  privateKey:
    algorithm: ECDSA
    size: 521
    encoding: PKCS8
    rotationPolicy: Always
  issuerRef:
    name: viktor.cloud-root-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io

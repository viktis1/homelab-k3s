apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpu-orchestrator
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gpu-orchestrator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gpu-orchestrator
    spec:
      serviceAccountName: gpu-orchestrator
      containers:
        - name: 
          image: 

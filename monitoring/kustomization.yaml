# Monitoring stack setup
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

labels:
- pairs:
    platform: monitoring
  includeSelectors: true
  includeTemplates: true

resources:
- resources/monitoring-fast-disk-grafana-data.persistentvolume.yaml
- resources/monitoring-fast-disk-prometheus-data.persistentvolume.yaml
- resources/monitoring.viktor.cloud-tls.certificate.cert-manager.yaml
- resources/monitoring.viktor.cloud.ingressroute.traefik.yaml
- resources/monitoring.namespace.yaml
- components/agent-kube-state-metrics
- components/agent-prometheus-node-exporter
- components/server-grafana
- components/server-prometheus

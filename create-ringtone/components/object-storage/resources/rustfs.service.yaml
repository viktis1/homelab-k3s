apiVersion: v1
kind: Service

metadata:
  name: rustfs

spec:
  selector:
    app.kubernetes.io/name: rustfs

  ports:
    - name: s3
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001